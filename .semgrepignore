# Pre-existing taint-analysis false-positives in the headless theme.
# Tracked separately: see follow-up issue.
#
# Specific findings under triage (all false-positives but blocking edits):
#   wordpress/themes/cdcf-headless/functions.php:1206,1221 — unlink($tmp)
#     where $tmp = $file . '.tmp.' . getmypid() (locally-constructed, not user input)
#   wordpress/themes/cdcf-headless/functions.php:3904,3906,3927 — echo of
#     count($post_ids) / count($target_langs) (already integers) and
#     wp_get_attachment_image() output (already sanitized HTML)
wordpress/themes/cdcf-headless/functions.php
